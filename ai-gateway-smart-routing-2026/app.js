(()=>{const p=document.querySelector('.progress');const scroll=()=>{const d=document.documentElement,m=d.scrollHeight-d.clientHeight;p.style.width=(m?100*d.scrollTop/m:0)+'%'};addEventListener('scroll',scroll,{passive:true});scroll();const links=[...document.querySelectorAll('.toc a')],secs=links.map(a=>document.querySelector(a.getAttribute('href'))).filter(Boolean);new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-15% 0px -70% 0px'}).observe(secs[0]);const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-15% 0px -70% 0px'});secs.forEach(s=>io.observe(s));const rows=[...document.querySelectorAll('.vendors tbody tr')],search=document.querySelector('#vendorSearch'),btns=[...document.querySelectorAll('.filter')];let active='all';const filter=()=>{const q=search.value.trim().toLowerCase();rows.forEach(r=>r.classList.toggle('hide',!((active==='all'||r.dataset.tags.split(' ').includes(active))&&(!q||r.textContent.toLowerCase().includes(q)))))};search.addEventListener('input',filter);btns.forEach(b=>b.onclick=()=>{active=b.dataset.filter;btns.forEach(x=>x.classList.toggle('active',x===b));filter()});const models=[{name:'Swift',note:'低成本通用',q:.58,c:.95,l:.9,r:.78,vision:0,long:0,code:0},{name:'Balance',note:'均衡通用',q:.8,c:.68,l:.66,r:.88,vision:1,long:1,code:0},{name:'Frontier',note:'高质量推理',q:.95,c:.28,l:.38,r:.92,vision:1,long:1,code:1},{name:'Coder',note:'代码专用',q:.88,c:.55,l:.58,r:.85,vision:0,long:1,code:1}],ids=['wq','wc','wl','wr'],box=document.querySelector('#models');function render(){const vals=ids.map(id=>+document.querySelector('#'+id).value),sum=vals.reduce((a,b)=>a+b,0)||1,w=vals.map(v=>v/sum);ids.forEach((id,i)=>document.querySelector('#'+id+'v').textContent=vals[i]);const req={vision:document.querySelector('#vision').checked,long:document.querySelector('#long').checked,code:document.querySelector('#code').checked};let best=null;const scored=models.map(m=>{const blocked=req.vision&&!m.vision||req.long&&!m.long||req.code&&!m.code,score=w[0]*m.q+w[1]*m.c+w[2]*m.l+w[3]*m.r;if(!blocked&&(!best||score>best.score))best={name:m.name,score};return{...m,score,blocked}});box.innerHTML=scored.map(m=>`<div class='model ${best&&m.name===best.name?'selected':''} ${m.blocked?'blocked':''}'><b><span>${m.name}</span><span class='score'>${m.blocked?'过滤':m.score.toFixed(3)}</span></b><small>${m.note}</small><div class='bars'>${[['质量',m.q],['成本',m.c],['时延',m.l],['可靠',m.r]].map(x=>`<div class='bar'><span>${x[0]}</span><div class='track'><div class='fill' style='width:${x[1]*100}%'></div></div><span>${Math.round(x[1]*100)}</span></div>`).join('')}</div></div>`).join('');document.querySelector('#result').textContent=best?`选择：${best.name}（效用 ${best.score.toFixed(3)}）`:'没有候选满足全部硬约束'}[...ids,'vision','long','code'].forEach(id=>document.querySelector('#'+id).addEventListener('input',render));render()})();